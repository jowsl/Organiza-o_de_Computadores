/* A - a divisão da formula, sqrt((pow(a,2)+pow(b,2)) /2) funciona porque 'pow' retorna em double
Se fosse sqrt( a*a + b*b ) / 2.0 ); tem que colocar o número que divide em inteiro no caso o  2.0

B - o comando += poderia ser usado
ex:
total = 25
total += (min-100) * 0.6;    ao invés do total = (min-100) * 0.6 + 25*/
